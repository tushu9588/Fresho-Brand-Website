## Packages
framer-motion | Essential for premium page transitions, scroll reveals, and micro-interactions

## Notes
- Ensure `/api/products` returns products with an `imageUrl` field (string) pointing to fresh food images.
- Unsplash image placeholders are used as fallbacks if the API images fail to load.
- Uses existing Shadcn UI components for rapid, accessible form and card building.
